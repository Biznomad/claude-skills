# {{CLIENT_NAME}} {{ROLE_TITLE}} — Hermes profile environment
# Created {{CREATED_AT}}

TELEGRAM_BOT_TOKEN={{TELEGRAM_BOT_TOKEN}}

# User gate
TG_ALLOWED_USER_IDS={{OWNER_TG_ID}}
TELEGRAM_ALLOWED_USERS={{OWNER_TG_ID}}
GATEWAY_ALLOW_ALL_USERS=false

# Group behavior — bot only responds when @mentioned, never lurks
TELEGRAM_REQUIRE_MENTION=true
TELEGRAM_OBSERVE_UNMENTIONED_GROUP_MESSAGES=false
TELEGRAM_EXCLUSIVE_BOT_MENTIONS=true

# Group lock — set via lock-group.sh after team room is created
# TELEGRAM_GROUP_ALLOWED_CHATS=<chat_id>
